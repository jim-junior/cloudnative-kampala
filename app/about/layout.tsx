export const metadata = {
  title: "About | Cloud Native Kampala",
  description:
    "Learn about Cloud Native Kampala, a CNCF community group dedicated to building a stronger cloud native community in Uganda through events, mentorship, and open source contributions.",
};

const layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default layout;
