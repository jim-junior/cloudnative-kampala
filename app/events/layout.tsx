export const metadata = {
  title: "Events | Cloud Native Kampala",
  description:
    "Discover upcoming meetups, workshops, and past event recordings from the Cloud Native Kampala community. Join us to learn Kubernetes, Docker, and more.",
};

const layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default layout;
